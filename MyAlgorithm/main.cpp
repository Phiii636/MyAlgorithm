#include "myAlgorithm.h"
#include <vector>
#include <string>

#include <algorithm>

int main()
{
	int a{ 10 }, b{ 20 };
	//cout << my::larger<int>(a, b,
	//	[&](int& x, int& y) { return x >= y; }) << endl;
}